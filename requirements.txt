fastapi>=0.115
uvicorn[standard]>=0.30
pandas>=2.2
numpy>=1.26
openpyxl>=3.1
python-multipart>=0.0.9
# El cliente HTTP con el que se le habla a Google durante el login. Estaba
# instalado de arrastre en la máquina de desarrollo —lo trae el TestClient de
# FastAPI— y por eso nadie lo notó hasta el primer despliegue en una máquina
# limpia, donde el servicio no arrancó: ModuleNotFoundError: No module named
# 'httpx'. Hay una prueba que compara los import del paquete contra este
# archivo para que no vuelva a pasar.
httpx>=0.27
cryptography>=42

# La fuente de datos de MetaTrader. SOLO WINDOWS: el paquete no existe para
# Linux ni Mac, así que el marcador de plataforma no es cosmético — sin él,
# una instalación en el servidor de Linux falla entera.
#
# Y no es obligatoria para que la aplicación arranque: si falta, la pantalla
# de datos ofrece Dukascopy y el módulo lo dice con esas palabras en vez de
# reventar con un ImportError.
MetaTrader5>=5.0.45; sys_platform == "win32"
